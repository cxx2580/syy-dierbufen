<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.mapper.ProductMapper">
    <resultMap id="BaseResultMap" type="com.erp.entity.Product">
        <id column="id" property="id"/>
        <result column="product_no" property="productNo"/>
        <result column="product_name" property="productName"/>
        <result column="specification" property="specification"/>
        <result column="unit" property="unit"/>
        <result column="price" property="price"/>
        <result column="stock" property="stock"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.erp.entity.Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (product_no, product_name, specification, unit, price, stock, status, create_time, update_time)
        VALUES (#{productNo}, #{productName}, #{specification}, #{unit}, #{price}, #{stock}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.erp.entity.Product">
        UPDATE product
        SET product_name = #{productName},
            specification = #{specification},
            unit = #{unit},
            price = #{price},
            stock = #{stock},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM product WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM product
        <where>
            <if test="productName != null and productName != ''">
                AND product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM product
        <where>
            <if test="productName != null and productName != ''">
                AND product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
        </where>
    </select>
</mapper>
