<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.mapper.SalesOrderMapper">
    <resultMap id="BaseResultMap" type="com.erp.entity.SalesOrder">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="customer_id" property="customerId"/>
        <result column="customer_name" property="customerName"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.erp.entity.SalesOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sales_order (order_no, customer_id, customer_name, total_amount, status, remark, create_time, update_time)
        VALUES (#{orderNo}, #{customerId}, #{customerName}, #{totalAmount}, #{status}, #{remark}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.erp.entity.SalesOrder">
        UPDATE sales_order
        SET customer_id = #{customerId},
            customer_name = #{customerName},
            total_amount = #{totalAmount},
            status = #{status},
            remark = #{remark},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sales_order WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sales_order WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM sales_order
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND order_no LIKE CONCAT('%', #{orderNo}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM sales_order
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND order_no LIKE CONCAT('%', #{orderNo}, '%')
            </if>
        </where>
    </select>
</mapper>
