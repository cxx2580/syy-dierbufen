<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.mapper.SupplierMapper">
    <resultMap id="BaseResultMap" type="com.erp.entity.Supplier">
        <id column="id" property="id"/>
        <result column="supplier_no" property="supplierNo"/>
        <result column="supplier_name" property="supplierName"/>
        <result column="contact" property="contact"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="address" property="address"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.erp.entity.Supplier" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO supplier (supplier_no, supplier_name, contact, phone, email, address, status, create_time, update_time)
        VALUES (#{supplierNo}, #{supplierName}, #{contact}, #{phone}, #{email}, #{address}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.erp.entity.Supplier">
        UPDATE supplier
        SET supplier_name = #{supplierName},
            contact = #{contact},
            phone = #{phone},
            email = #{email},
            address = #{address},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM supplier WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM supplier WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM supplier
        <where>
            <if test="supplierName != null and supplierName != ''">
                AND supplier_name LIKE CONCAT('%', #{supplierName}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM supplier
        <where>
            <if test="supplierName != null and supplierName != ''">
                AND supplier_name LIKE CONCAT('%', #{supplierName}, '%')
            </if>
        </where>
    </select>
</mapper>
